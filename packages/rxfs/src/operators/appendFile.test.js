import {expect} from 'chai';
import {marbles} from 'rxjs-marbles/mocha';

import appendFile from './appendFile';

describe('appendFile', () => {
  it('should correctly call file append workflow', () => {
    expect(false).to.be.true;
  });
});
