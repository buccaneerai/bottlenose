import {expect} from 'chai';

import wsManager from './wsManager';

describe('wsManager', () => {
  it('should reconnect and emit reconnect event when the connection is interrupted', () => {

  });

  it('should ', () => {

  });
});
