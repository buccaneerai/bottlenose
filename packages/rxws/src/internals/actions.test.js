import {expect} from 'chai';

import {
  CONNECT_ERROR,
  connectError
} from './actions';

describe('actions', () => {
  // it('connectError should return an object with {socket,error,type:CONNECT_ERROR}', () => {
  //   const error = new Error('money in the banana stand');
  //   const socket = {iAmA: 'socket'};
  //   const params = {error, socket};
  //   const actual = connectError(params);
  //   expect(actual).to.deep.equal({type: CONNECT_ERROR, socket, error});
  // });
});
